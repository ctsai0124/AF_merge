# AF 用人費用欄位排序系統 — 專案交接說明

> 給新對話用的背景說明。這是一個已經上線運作的系統，不是從零開始。

---

## 一、這是什麼

高雄市教育局人事單位使用的內部工具，處理各校薪資稽核作業。

**使用者**：人事人員（作者本人與同事），非技術背景
**部署**：Zeabur（Flask），網址 `https://aftool.zeabur.app`
**原始碼**：GitHub `ctsai0124/AF_merge`

### 兩大功能

1. **排序**（原有功能）— 依固定清冊的序號重新排列 AF 資料欄位，產製下載檔與稽核表
2. **比對**（後來新增）— 核對 AF 資料與學校薪資清冊的金額是否相符

---

## 二、資料來源與業務規則

| 檔案 | 內容 | 說明 |
|---|---|---|
| **固定清冊**（.xlsx） | 序號、姓名、身分證字號 | 人員排列順序，使用者自行維護 |
| **AF 校對清冊**（.xls） | **僅正式人員** | 由「AF 用人費用管理系統」產製 |
| **薪資清冊**（.pdf） | **全校人員**（含非正式） | 各校自行製作，格式不一 |

### 核心業務規則

```
AF ＝ 正式人員（完整）
薪資清冊 ＝ 全校人員（含代理、長代、教保員等）

所以「清冊有、AF 沒有」有兩種意義：
  ├─ 非正式人員 → 正常，預設收合
  └─ 正式人員   → ⚠️ AF 可能漏建，必須列出（這是稽核重點）
```

**這條規則是整個系統的設計核心，不可違背。**

### AF 欄位對應（實際驗證過）

| 薪資清冊項目 | AF 來源 |
|---|---|
| 薪俸／本俸 | `支領數額`（薪俸表別段）|
| 專業加給／學術研究費 | `支領數額.1` |
| 職務加給／主管加給 | `支領數額.2`，且 `職務加給表別` = C1009 或 C1001 |
| 導師加給＋特教加給 | `支領數額.2`，且 `職務加給表別` = **C1014**（AF 把兩者合併為一筆）|

- AF 一個人可能有**多列**（同時有主管職與導師職時），須依身分證字號合併
- AF 的 `單位` 欄即處室（校長室、教務處、學生事務處、輔導室…）

---

## 三、專案檔案

### Zeabur（伺服器）

```
專案根目錄/
├─ app.py              Flask 路由、佇列、計數器、排除設定
├─ paycheck.py         比對核心：AF 讀取、PDF 解析、配對、比對
├─ mac_ocr/            Mac 現役 worker、直橫式解析器與 Vision 程式的版本備份
├─ requirements.txt    Flask / pandas / openpyxl / xlrd / python-docx / pdfplumber
├─ templates/
│  └─ index.html       單頁介面（步驟 1 上傳 → 2 執行 → 3 比對結果 → 4 排序結果）
└─ static/             school.xlsx、稽核表-.docx、固定清冊範例.xlsx、單機版.xlsm
```

**環境變數**

| 變數 | 值 | 用途 |
|---|---|---|
| `DATA_DIR` | `/data` | 掛載 Volume，存計數器與設定（不設會在重新部署後歸零）|
| `OCR_KEY` | 隨機字串 | Mac 端辨識服務的驗證密鑰；**不設則整個 OCR 功能停用**，掃描檔顯示警語 |

### Mac 端（辨識服務，選配）

作者有一台 24 小時開機的 Mac，用 macOS Vision 框架做掃描檔文字辨識。

```
~/payroll-ocr/
├─ ocr_extract.swift   用 Vision 辨識 PDF，輸出每個文字片段與座標的 JSON
├─ parse_tokens.py     把 token JSON 還原成人員資料（支援直式／橫式）
├─ worker.py           輪詢伺服器、辨識、回傳。
│                      ⚠ 必須呼叫 parse_tokens() 主函式（自動判斷直式／橫式）。
│                      曾因寫死 parse_row（橫式）導致直式清冊全毀，見第八節。
└─ config.json         { "server": "...", "key": "..." }  ← 含密鑰，權限 600
~/Library/LaunchAgents/com.payroll.ocr.plist   開機自動啟動
```

**注意**：新版 macOS 要用 `launchctl bootstrap gui/$(id -u) <plist>`，
`launchctl load` 已失效。

---

## 四、比對流程

### 有文字層的 PDF

```
上傳 AF ＋ 薪資清冊 PDF
   ↓ 自動判斷版面（直式／橫式）
   ↓ 解析 → 加總自檢 → 標記非正式人員
   ↓ 配對（身分證 → 姓名 → 單位 → 主管職 → 金額指紋 → 人工）
   ↓ 比對四個項目
顯示結果
```

### 掃描圖檔

```
上傳 → 偵測無文字層 → 顯示警語
   ├─ 未設 OCR_KEY：只有警語，請使用者索取電子版
   └─ 已設 OCR_KEY：警語下方多一顆「改用文字辨識讀取」按鈕
        ↓ 使用者按下才進佇列（檔案先保留，10 分鐘逾時清除）
        ↓ Mac 輪詢領取 → Vision 辨識 → 回傳
        ↓ 加總相符且姓名對得上 → 直接比對
        ↓ 其餘 → 帶著辨識值列出，使用者對照紙本修正
```

**Mac 輪詢節奏**由伺服器決定：有人使用網站時 3 秒、閒置 60 秒。

---

## 五、重要設計決策（含慘痛教訓）

### 1. 絕不用金額反推姓名 ⚠️

**曾經因此產生嚴重錯誤。** 原本在姓名對不上時會用「四項金額完全相同」來配對，
結果把離職的林怡慈與接任同職務的楊棨翔判定為同一人，人員異動完全被掩蓋。

人員異動時接任者常沿用相同職務與薪級，金額一模一樣是常態。
**姓名不同就是不同人**，寧可列為「查無此人」交由使用者判斷。

姓名模糊比對也只接受「字數相同、僅一字之差」（吳盂謙→吳孟謙可以，
楊棨翔→林怡慈不行）。

### 2. 職稱只看「兼」之前的部分

「教師兼代理主任」的主要職務是教師，是正式人員，**不可**因含「代理」被排除。
「代理教師」才是非正式。

### 3. 正式職稱永不自動收合

`NEVER_EXCLUDE_TITLES`：校長、主任、組長、教師、導師、科任、幹事、書記、
護理師、校護、工友、技工、駕駛、教練、職員、管理員

即使某月 AF 漏建，統計上「教師」看起來像非正式人員，也絕不學習收合——
否則 AF 漏建這種最該抓的錯誤將永遠無法被發現。

### 4. 不移除、只標記

被判定為非正式的人員**仍然完成比對**，只是在介面上預設收合
（`_次要` 標記）。資料完整，隨時可展開。

收合區的標題若含金額不符者會標示「其中 N 人金額不符」，
避免算出來了卻完全不提。

### 5. 加總自檢是最後防線

`薪俸 + 主管加給 + 專業加給 + 導師特教 + 地域 + 其他 − 扣款 = 應發金額`

不符者一律不採用，交由使用者對照紙本確認。**絕不自行猜測補值。**

實際攔下過真實的辨識錯誤（`2,000` 被讀成 `2,0001`）。

### 6. 使用者介面不顯示記憶機制

作者要求不讓使用者知道系統在記憶什麼。學習在背景進行，
介面上只有「另有 N 人未列入本次稽核（點擊展開）」。

管理用途可查 `/settings/title-stats`。

### 7. AF 有配對到的人，永不收合（2026/7 新增）

排除規則（約僱、教保員等）只適用於「清冊有、AF 無」的人。
**只要配對到 AF，一律列入主清單**——AF 有建就代表屬於稽核範圍，
即使職稱命中排除清單（例：AF 有建的約僱人員陳佳妍）。
實作於 `paycheck.py` 的 `compare()`：配對成功者強制 `次要=False`。

收合區每人另有「**納入本次稽核**」按鈕（`index.html`），按下即移入
主清單顯示，可「移回未列入」反悔。純顯示層操作：不改排除設定、
不影響背景學習、不需重跑辨識。

### 8. 依學校記住成功版面，但不可永久寫死（2026/7 新增）

掃描檔工作會依 AF 檔名取得學校機關代碼。伺服器在 OCR 結果中有至少
50% 的人能以「姓名完全相符或有效身分證」對應 AF 後，才把本次成功使用的
`vertical`／`horizontal` 寫入 `/data/layout_profiles.json`。

下次同校工作領取時，伺服器把設定當作 `layout_hint` 送給 Mac。Mac 仍會同時
解析直式與橫式，依姓名合理性、加總、身分證與表格結構評分；舊設定只加分，
不會永久強制。這可兼顧同校穩定性，也能在學校更換薪資報表格式時自動切換。

設定檔不保存姓名、身分證、金額或 OCR token。

若解析結果的加總通過率低於 50%，或出現多個疑似薪資項目名稱，
Mac worker 會把該次原始 token 存到 `~/payroll-ocr/diagnostics/`，
檔案權限為 600、資料夾權限為 700，最多保留最近 10 份。檔名只含時間、
工作編號與版面，不含學校或人名；此檔仍含清冊內容，不可外傳。

---

## 六、排除與學習機制

### 三層

| 層級 | 內容 | 生效 |
|---|---|---|
| 內建清單 | 代理、代課、長代、教保員、助理教保、約僱、約聘、臨時、工讀、替代役、實習、志工 | 立即 |
| 自動學習 | 該校清冊有、但 AF 從未出現過的職稱 | 累積 2 份檔案、3 人次後 |
| 手動 | 使用者勾選職稱或個別排除 | 立即 |

**記的是職稱不是人名**，所以代理教師換人也照樣適用。
設定依學校分開（用 AF 檔名裡的機關代碼，如 `397084400Y`）。

儲存於 `/data/exclusions.json` 與 `/data/title_stats.json`。

### 已驗證的判斷

用蔡文國小 36 種職稱實測，**零錯誤**：

- 正確排除：長代、侍親長代、育嬰長代、教保員
- 正確保留：專任教練、借調、資源班、幼師、輔導教師（都是正式人員、都在 AF）

---

## 七、薪資清冊 PDF 的三種型態

| 型態 | 判斷方式 | 處理 |
|---|---|---|
| 有文字層 | `pdfplumber` 抽得到文字且有表格 | ✅ 自動解析 |
| 掃描圖檔 | 抽不到文字 | 顯示警語，或走 Mac OCR |
| **OCR 轉檔過的** | 有文字但 `extract_tables()` 為 0 | ❌ 表格結構已散，顯示專屬警語 |

**第三種特別重要**：作者曾試過用 Google 文件 OCR 轉檔，文字讀得出來但
欄列關係全毀，程式無法解析。使用者看到檔案有文字會以為能用，
所以有專門的說明訊息。

### 兩種版面

**橫式**（三侯國小）：一人一列
```
編號 職稱 員工姓名 俸點 薪俸 主管加給 學術研究 導師費 … 應發金額
```

**直式**（三埤、竹滬國小）：一人一欄，左側為項目名稱
```
序號 / 身分證字號 / 姓名 / 職稱 / 支薪俸級
本俸 / 專業加給 / 職務加給 / 導師加給 / 特教加給 / 應發數合計 …
```

直式清冊常有多頁，**每頁各有自己的「姓名」列**，須依頁碼分區解析。

---

## 八、Mac 端解析的踩雷紀錄

這部分花了最多時間除錯，記錄下來避免重蹈覆轍。

### 1. 姓名靠左、金額靠右

不能用「最接近的中心點」配對欄位，金額會被歸到隔壁的人。
正解：用「每個人都有值」的金額列（應發數合計或本俸）建立欄位座標，
姓名依左至右順序對應。

### 2. 數字被切成兩段

「55,690」常被讀成 `'55,'` 與 `'690.'` 兩個 token。更麻煩的是排序後
**中間可能夾著鄰欄的完整數字**，所以不能只比較相鄰 token。

正解：找出所有以逗號結尾的碎片，各自配對右側最接近的三位數字。

### 3. 分隔字元有各種變體

全形逗號、頓號、句點、間隔號都可能出現，須正規化。
但要注意：**尾段可能帶多餘標點**（`'690.'`），正規化後不可要求「純三位數字」，
否則反而配對不到（這個坑我自己踩過）。

### 4. 過濾字詞要精確

`SKIP_WORDS` 曾誤含「總務」「人事」「會計」「出納」，
導致總務主任、人事主任、會計主任、出納組長**全被跳過**。

現在只留「不可能出現在職稱中」的詞：小計、合計、總計、備註、製表、
用印、機關首長、承辦人、核章、稽核人員。

### 5. 身分證字號有檢查碼

台灣身分證可用檢查碼驗證，能偵測 OCR 讀錯。
實測 33 組真實號碼全數通過，故意改錯一碼則全被抓出。

有身分證時配對最可靠——**姓名讀錯也不影響**。

### 診斷工具

```bash
python3 parse_tokens.py tokens.json --diagnose      # 版面、欄位對應、對齊檢查
python3 parse_tokens.py tokens.json --person 姓名   # 某人各項目的原始 token 與座標
python3 parse_tokens.py tokens.json | grep "✗"      # 只列加總不符者
```

`--person` 會用 `repr()` 顯示原始字元，前面好幾個 bug 都是靠它才現形的。
**遇到解析問題先看診斷，不要猜。**

### 6. worker.py 曾寫死橫式解析（2026/7 修正）⚠

`parse_tokens.py` 明明有完整的直式解析與版面判斷，但 worker.py 曾寫成
`parse_row(r) for r in group_rows(tokens)`——**寫死逐列橫式解析、
從未呼叫版面判斷**。三侯（橫式）剛好沒事，竹滬（直式）一來就把
項目名稱列（本俸、健保費…）的第一欄當成姓名，整批全毀。

已改為呼叫 `parse_tokens()` 主函式（自動判斷版面），並印出
「版面判定：vertical/horizontal」供除錯。驗證：
`grep -c "parse_all" worker.py` 應為 2、`grep -c "parse_row"` 應為 0。

### 6-1. 版面判定只看前 25 列會誤判（2026/7 修正）⚠

旗山區鼓山國小的直式掃描清冊曾被判成 `horizontal`，結果把「健保費、
所得稅、儲蓄存款、實發數」當作姓名。原因是舊 `detect_layout()` 只檢查
前 25 列；不同學校的頁首較長時，真正的「姓名／本俸」列可能落在後面。

現版 `mac_ocr/parse_tokens.py` 會檢查全部頁面、同時產生直式與橫式候選，
並把薪資項目名稱被當成人名視為強烈負分。搭配依學校保存的版面提示，
但仍保留換版時的自動回退。

### 6-2. PDF 旋轉頁面畫布裁切與鼓山堆疊表頭（2026/7 修正）⚠

旗山區鼓山國小 PDF 的每頁 `/Rotate` 都是 270°，原始 MediaBox 為
842×595。舊 `ocr_extract.swift` 仍建立 842×595 橫向畫布，再套用旋轉後
只剩清冊下半部，姓名、職稱、薪俸全部被裁掉。現版遇到 90°／270°會交換
畫布寬高為 595×842，確保完整頁面進入 Vision。

鼓山各頁的表頭又會被 OCR 讀成不同變體，例如「姓名」變成「上.名／生名／
E名」，「本俸」變成「太俸／x俸／5俸」。現版直式解析器在標準欄名失敗時，
會用表頭區的連續人名、金額欄座標及本俸起算的固定資料列順序還原，不依賴
每頁欄名完全正確。

真實完整 token 驗證：50 人全部解析，49 人加總相符；陳秋媛的導師費
`4,000` 被 OCR 讀成 `40,001`，由加總檢查攔下並交人工確認，屬正確行為。

### 7. 多個 worker 同時在跑會搶工作 ⚠

worker 手動跑在 Terminal 時，重複啟動、開了忘記關，會造成
**多個 worker（可能新舊版混雜）同時輪詢搶工作**——誰搶到誰做，
症狀是「同一份檔案有時解對、有時全毀」，極難察覺。

改完 worker.py 後：`pkill -f worker.py` 全部停掉 →
`ps aux | grep worker.py | grep -v grep` 確認清空 → 只啟動一個。
（Python 啟動時即載入程式碼，改檔案不會影響已在跑的程序。）
根治方式是改用 launchd 管理（保證單一實例、開機自動啟動）。

### 8. 伺服器會快取辨識結果 ⚠

同一份 PDF 重測時，伺服器可能直接回上次存的結果、**不會重新派工**
（症狀：結果秒出、Mac worker 沒印「領到工作」）。修正 Mac 端後
重測前，須先在 Zeabur 控制台 Restart 服務清掉舊結果。

### 9. AnyDesk 傳檔落在家目錄

AnyDesk 檔案傳輸把檔案放到 Mac **家目錄**，不會進 `~/payroll-ocr/`。
swift 報「無法開啟 PDF」十之八九是檔案根本不在工作目錄——
先 `ls -lt ~/*.pdf` 找到檔案再 `cp` 過去。另注意家目錄可能殘留
同名舊檔（parse_tokens.py、payroll1.pdf 等），診斷時認明
`~/payroll-ocr/` 的才是現役版本。

---

## 九、目前狀態

### 已完成並驗證

- 排序功能（原有，未動過核心邏輯）
- 有文字層 PDF 的自動比對（直式、橫式皆可）
- 同名區辨（身分證 → 職稱 → 單位 → 主管職）
- 掃描檔警語（含 OCR 轉檔的專屬說明）
- 使用次數計數器（頁首右上）
- 排除與學習機制
- Mac OCR 服務：橫式 17/17、直式 33/34
- **竹滬（直式掃描檔）網站端全流程**：27 人一致、5 人正確收合
  （2026/7/25，修正 worker.py 版面判斷後驗證通過）
- **「AF 有配對即列入主清單」規則與「納入本次稽核」按鈕**
  （paycheck.py + index.html，見第五節第 7 條）
- **依學校保存 OCR 成功版面與安全回退**
  （app.py + mac_ocr/worker.py + mac_ocr/parse_tokens.py）
- **鼓山（旋轉掃描檔）多頁直式解析**：50 人、50 人加總相符
  （2026/7/25；`4,000` 被 OCR 讀成 `4,0001` 的一筆，已用低信心、
  同列重複值及字框寬度辨識為框線雜訊；未使用加總反推）
- 人工確認畫面的「整類排除」只顯示非正式職稱；教師、導師、教練等
  正式職稱不再反覆詢問，也會過濾舊版曾誤存的正式職稱設定
- OCR 姓名遇到特殊字碼或單字誤判時，可在「至少兩字同位置一致、
  清冊四項加總正確、AF 姓名候選唯一」三項條件都成立時採用 AF
  標準姓名；AF 本來沒有的人仍保留為稽核差異
- **蔡文國小橫式掃描清冊相容（Mac v9）**：姓名改以薪點鄰接位置定位，
  避免把資源班、暫代、教練等職別當姓名；薪資小計以前綴和定位，
  正確拆分主管／特殊職務、專業、導師／特教及其他加給。教保費等
  「其他加給」也會納入 OCR 驗算，但不冒充 AF 四個比對欄位
- **蔡文實機 v9 結果**：73 人中網站 72 人自動通過，只剩洪一茜；
  v10 再修正「小額特殊職務加給位於專業加給之前」的無小計備援，
  並在任何一筆驗算不符時保存 Mac 本機診斷，方便追查漏讀欄位
- **蔡文實機 v10 結果**：洪一茜四個薪資欄已正確，但 OCR 漏讀應發
  62,630，舊備援誤取後方保險金額 3,107。v11 會拒絕小於薪俸的
  應發候選；暫以各薪資項目合計填入並明確標記為推算，保留人工確認
- **蔡文橫式共用欄**：薪資冊的第一個小額欄同時標示「主管加給／
  特教職加」。v12 依職稱分流：校長、主任、組長歸主管加給；非主管
  歸特教加給，並與後方導師費相加後送入 AF 的「導師＋特教」欄
- **清楚小計仍可能被 Vision 整格漏掉**：診斷
  `20260725-140958_096ce8880d68_horizontal.json` 證實洪一茜列沒有
  輸出 62,630 token（且相鄰補助數字被黏在一起），並非分列容差。
  網站 `.8` 僅在身分證精確對應，或姓名至少兩字同位置一致且 AF 四欄
  逐項完全相符、候選唯一、沒有其他加給時，接受項目合計並自動通過；
  其餘仍要求人工確認。v13 另保留 5,930 等主管加給的金額判斷，避免
  OCR 將「主任」截短後誤併入特教

### 已知限制

- 掃描檔若將右側框線黏成尾端數字（例如 `4,000` → `4,0001`），
  僅在低信心、同列至少兩個相同合法值、字框寬度相近三項證據都成立時
  去除框線雜訊；否則拒絕該金額並留給人工確認。系統不依驗算結果猜值
- 遇到全新版面可能需要調整 `FIELD_ALIAS`（多數情況只是用詞不同，加一行即可）

### 待處理

- ⚠ **竹滬報表有一筆姓名待修正**：收合區「林欣樺 契約教保員」
  實為林**宛**樺（D222408917）——人工確認時沿用了 OCR 誤讀值。
  同校另有正式主任林欣樺（S222264748），一字之差、兩人皆真實存在。
  下次跑竹滬人工確認時改正姓名再送出。
- Mac worker 的 launchd 開機自動啟動（`launchctl bootstrap`）——
  設好可根治「多重 worker 搶工作」問題（見第八節第 7 條）
- 固定清冊中的林淑芳在 AF 找不到對應：確認是已異動（更新固定清冊）
  還是 AF 漏建（稽核重點）

---

## 十、與作者互動的注意事項

- 作者是**人事人員不是工程師**，指令要能直接複製貼上，路徑要具體
- 作者用 **AnyDesk 從 Windows 遠端操作 Mac**，傳檔案要說明清楚
  （常見錯誤：傳到家目錄後忘了 `cp` 到 `~/payroll-ocr/`）
- 每次改動後要**確認版本真的換上去**（用 `grep -c` 檢查特徵字串）
- 這是**稽核工具**，正確性優先於便利性。寧可多問一句，
  不要讓系統自作主張猜測或隱藏資料
